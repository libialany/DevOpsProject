name: iris_project

entry_points:
  main:
    parameters:
      test_size: {type: float, default: 0.2}
      random_state: {type: int, default: 42}
    command: "python iris_train.py --test_size {test_size} --random_state {random_state}"
