name: Manual Trigger Workflow with loglevel & tags
on:
  workflow_dispatch:
    branches:
      - notsubcribers
jobs:
  print-tag:
    runs-on: ubuntu-latest
    steps:
      - name: Print the purpose of run
        run: echo  "hi there"