apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}
  labels:
    app: demo

spec:
  type: {{ .Values.service.type }}

  selector:
    app: demo

  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.containerPort }}
